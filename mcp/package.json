{
  "name": "artifact-mimicry-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "echo 'Deploy through the GitHub Actions workflow so the OAuth KV binding is resolved safely.' && exit 1",
    "test": "node --test"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7",
    "@cloudflare/workers-oauth-provider": "0.8.2",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "0.16.0",
    "ai": "^6.0.0",
    "fflate": "^0.8.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
